services:
  - type: web
    name: quote-online-workbench
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: APP_PASSWORD
        sync: false
      - key: ADMIN_PASSWORD
        sync: false

# Quote Online Workbench

Online quotation workbench with:

- shared quote number generation
- product lookup by stock code
- admin Excel price-table upload
- quote saving
- admin seal upload

Set these environment variables before deployment:

- `APP_PASSWORD`: normal user access password
- `ADMIN_PASSWORD`: administrator password for uploading price lists and seal image
